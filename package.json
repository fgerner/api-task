{
  "name": "api-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "JUNIT_REPORT_PATH=test-result/result.xml JUNIT_REPORT_STACK=1 mocha --timeout 25000 --colors --reporter mocha-jenkins-reporter || true",
    "test:previev": "NODE_ENV=previev mocha --timeout 25000 --colors --reporter mocha-jenkins-reporter || true",
    "start:demo": "NODE_ENV=demo mocha --timeout 25000 --colors --reporter mocha-jenkins-reporter || true"

  },
  "keywords": [],
  "author": "Fred Gerner",
  "license": "ISC",
  "dependencies": {
    "body-parser": "1.19.0",
    "chai": "4.2.0",
    "mocha": "6.2.0",
    "mocha-jenkins-reporter": "0.4.1",
    "supertest": "4.0.2"
  }
}
